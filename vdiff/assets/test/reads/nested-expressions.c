
int min(int x, int y) {
  if (x <= y) { return x; }
  else { return y;}
}

int max(int x, int y) {
  if (x >= y) { return x; }
  else { return y; }
}

int add(int x, int y) {
  return (x + y);
}

int main() {

  int x = 1;
  int y = 2;
  int* arr[3];

  if (min(x,y) > 2) {; }

  if (arr[min(1,x)]){}

  if (arr[0] == 1) {}
}

