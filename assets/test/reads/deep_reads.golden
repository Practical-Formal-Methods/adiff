int main()
{
    int x, y, z;
    __VERIFIER_read(x);
    if (x < 0)
    {
        __VERIFIER_read(y, z);
        while (y + z == 3)
        {
            int z;
            __VERIFIER_read(z);
            z = z + 1;
            for (int i = 0; i < 5; i++)
            {
                __VERIFIER_read(z);
                z = z + 3;
            }
        }
    }
}