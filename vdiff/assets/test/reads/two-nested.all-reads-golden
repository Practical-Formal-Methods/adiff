int f(int x)
{
    return x;
}
int main()
{
    int x, y;
    {
        {
            __VERIFIER_read(f, x);
            f(x);
        }
    }
    {
        {
            __VERIFIER_read(f, y);
            f(y);
        }
    }
}