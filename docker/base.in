# We base our image on the klee image as they have quite hefty requirements
FROM ubuntu:16.04

USER root
RUN apt-get update && \
#    apt-get -y upgrade && \
    apt-get -y install wget cbmc  git subversion clang-5.0 cmake build-essential ncurses-dev libgmp-dev libboost-system-dev unzip

RUN export MAKEFLAGS=-j$(nproc)

# RUN curl -sSL "https://get.haskellstack.org/" | sh

RUN mkdir -p "/root/.local/bin"
