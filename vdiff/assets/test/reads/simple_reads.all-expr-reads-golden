void foo(int x)
{
}
void bar();
int main()
{
    int x, y, z;
    __VERIFIER_read(x > 0, x);
    if (x > 0)
    {
        int a;
        foo(3);
        __VERIFIER_read(y, y + 1, y);
        y = y + 1;
        bar();
        __VERIFIER_read(x, x + 1, x);
        x = x + 1;
        __VERIFIER_read(z, z + 1, z);
        z = z + 1;
    }
}